rm.py: removes specified arguemnts (files or directories) from relative or absolute paths and then places them in ~/rm_trash.
restore.py: restores arguments passed to rm.py by taking in absolute paths of arguments that have been removed.

MUST PASS "-r" AS AN ARGUMENT WHEN REMOVING DIRECTORIES
